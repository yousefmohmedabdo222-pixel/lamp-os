# دليل التثبيت السريع

ملاحظة سريعة: هذا الدليل يشرح كيفية تحضير USB وتثبيت LAMP OS على جهاز حقيقي (UEFI أو BIOS).

- المتطلبات المسبقة:
	- احفظ نسخة احتياطية من أي بيانات مهمة على الجهاز المستهدف.
	- أداة لحرق الـ ISO إلى USB: على ويندوز استخدم Rufus، على لِينكس استخدم `dd` أو Etcher.
	- المستهدف يجب أن يدعم إقلاع x86_64. المثبّت يحتاج على الجهاز المستهدف بعض الأدوات مثل `parted`, `grub-install`, و أدوات `mkfs.*` (معظم الأجهزة الحديثة تحتوي عليها أو يمكن تركيبها من بيئة إنقاذ).

- تحضير الفلاشة (Rufus):
	1. اختر ملف `lamp-os-complete-installer.iso`.
 2. في Rufus اختر نمط التقسيم المناسب (GPT للـ UEFI، MBR للـ BIOS/Legacy) أو استخدم خيار "DD image" إذا طُلب.
 3. اضغط Start وانتظر انتهاء النسخ.

- خطوات التثبيت على الجهاز المستهدف:
	1. اقلع من الـ USB الذي حرقته.
 2. في قائمة الإقلاع اختر "Live" أو "Install" إذا ظهرت.
 3. إذا لم يبدأ المثبّت تلقائياً، افتح طرفية واكتب `/usr/local/bin/setup-wizard.sh` ثم Enter.
 4. اختر القرص الهدف واتبع التعليمات. ستُطلب منك تأكيد العمليات المدمرة (اكتب YES عند المطالبة) قبل إجراء أي فرمتة.
 5. المثبّت يدعم وضع التقسيم التلقائي و/أو اليدوي، ويقوم بكتابة GRUB تلقائياً.
 6. بعد الانتهاء أعد التشغيل وأزل الـ USB.

- ملاحظات مهمة:
	- الـ ISO صغير الحجم لأن النظام مصغّر: نواة خفيفة + BusyBox + واجهة TUI. مساحة التثبيت المتوقعة بعد التثبيت تتراوح عادة بين 350–500 ميجابايت.
	- التوقيع على Secure Boot وتهيئة تشفير القرص لم تُنفذ هنا — إن رغبت بالتشفير أو Secure Boot اتبع دليلًا مستقلًا.
	- إذا أبلغ المثبّت عن فقد أدوات مثل `parted` أو `grub-install`، يمكنك تشغيل بيئة إنقاذ/استعادة تحتوي هذه الأدوات أو تركيبها مؤقتاً ثم إعادة التشغيل للمحاولة مجدداً.

- استكشاف المشاكل الشائعة:
	- لا تظهر قائمة التثبيت: تأكد من الإقلاع من USB الصحيح أو جرّب وضع Legacy/UEFI مختلف.
	- فشل تثبيت GRUB: تأكد أن الجهاز يحتوي على `grub-install` وأنك تستخدم صلاحيات الجذر.

للمساعدة أو الإبلاغ عن مشكلة، افتح Issue في مستودع المشروع مع تفاصيل الجهاز والخطأ.
